# 2026-06-06 UI Pass Backups

- `20260606-current-ui-pass`: snapshot from before the current command-dock/hand-layout pass. Use this first if today's changes feel worse.
- `20260606-after-command-dock-hand-layout-pass`: snapshot after the current pass.

Each folder contains copies of the touched UI/render files so a single file can be compared or restored without touching unrelated work.
